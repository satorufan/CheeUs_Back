<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fullstack.finalproject.board.mapper.BoardMapper">

	<resultMap id="boardResultMap" type="com.fullstack.finalproject.board.dto.BoardDto">
		<id property="id" column="id" />
		<result property="authorId" column="author_id" />
		<result property="category" column="category" />
		<result property="title" column="title" />
		<result property="content" column="content" />
		<result property="writeday" column="writeday" />
		<result property="views" column="views" />
		<result property="like" column="likes" />
		<result property="replCnt" column="repl_cnt" />
		<result property="boardId" column="board_id" />
		<result property="photoes" column="photoes" />
		<result property="head" column="head" />
		<result property="media" column="media" />
		<result property="createdAt" column="created_at" />
	</resultMap>

	<select id="findAll" resultMap="boardResultMap">
		SELECT * FROM board;
	</select>

	<select id="findById" parameterType="int" resultMap="boardResultMap">
		SELECT * FROM board WHERE id = #{id};
	</select>

	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO board (author_id, category, title, content, writeday, views, likes, repl_cnt, board_id, photoes, head, media, created_at)
		VALUES (#{authorId}, #{category}, #{title}, #{content}, #{writeday}, #{views}, #{like}, #{replCnt}, #{boardId}, #{photoes}, #{head}, #{media}, #{createdAt});
	</insert>

	<update id="update">
		UPDATE board
		SET author_id = #{authorId}, category = #{category}, title = #{title}, content = #{content}, writeday = #{writeday}, views = #{views}, likes = #{like}, repl_cnt = #{replCnt}, board_id = #{boardId}, photoes = #{photoes}, head = #{head}, media = #{media}, created_at = #{createdAt}
		WHERE id = #{id};
	</update>

	<delete id="delete" parameterType="int">
		DELETE FROM board WHERE id = #{id};
	</delete>

</mapper>
